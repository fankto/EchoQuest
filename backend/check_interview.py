from app.db.session import async_session; import asyncio; from sqlalchemy import select; from app.models.models import Interview; async def get_interview(): async with async_session() as session: result = await session.execute(select(Interview).where(Interview.status == 'TRANSCRIBED')); interview = result.scalars().first(); print(f'Interview ID: {interview.id}'); print(f'Questionnaire ID: {interview.questionnaire_id}'); print(f'Has questionnaire: {bool(interview.questionnaire_id)}'); asyncio.run(get_interview())
